# Scan.io 🧾

**Scan.io** は、レシートをスキャンしてスマートに共有できる「割り勘 & コミュニケーション」アプリです。
Google Gemini (AI) を活用して面倒な入力を自動化。3Dリアクションやコメント機能で、お金の管理を「事務作業」から「楽しいコミュニケーション」に変えます。
共同生活や旅行、イベントなど、誰かとのお金のやり取りに最適です。

## ✨ 特徴 (Features)

* **AIレシートスキャン:** Google Gemini 1.5 Flash を使用し、カメラで撮るだけで「店名・金額・日付・カテゴリ」を自動入力。
* **スムーズな割り勘:** 月ごとの支出を集計し、「どちらがいくら払うべきか」を自動計算。
* **3Dリアクション:** 履歴アイテムに対して、動く3D絵文字でリアクション（❤️, 👍, 🎉, 🙏）。
* **ミニチャット:** 買い物ごとにコメントを残し、会話が可能。
* **PWA対応:** ホーム画面に追加して、ネイティブアプリのように動作。
* **洗練されたUI:** グラスモーフィズムとスレートカラーを基調とした、シンプルでモダンなデザイン。

## 🛠️ 技術スタック (Tech Stack)

* **Framework:** Next.js 14 (App Router)
* **Language:** TypeScript
* **Styling:** Tailwind CSS
* **Database:** Supabase (PostgreSQL)
* **AI:** Google Generative AI SDK (Gemini 1.5 Flash)
* **Icons:** Lucide React
* **Deployment:** Netlify / Vercel

## 🚀 ローカルでの実行方法 (Setup)

1.  **リポジトリのクローン**
    ```bash
    git clone [https://github.com/KouSei089/scan.io.git](https://github.com/KouSei089/scan.io.git)
    cd scan.io
    ```

2.  **依存関係のインストール**
    ```bash
    npm install
    ```

3.  **環境変数の設定**
    `.env.local` ファイルを作成し、以下のキーを設定してください。
    ```env
    NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
    NEXT_PUBLIC_GOOGLE_API_KEY=your_gemini_api_key
    ```

4.  **開発サーバーの起動**
    ```bash
    npm run dev
    ```
    http://localhost:3000 にアクセスして確認できます。

## 📂 データベース設定 (Supabase SQL)

`expenses` テーブルの定義:

```sql
create table expenses (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  store_name text,
  amount integer,
  purchase_date date,
  paid_by text,
  category text,
  reactions jsonb default '{}'::jsonb,
  comments jsonb default '[]'::jsonb
);